package model

func TestStream(t *testing.T) {
	// FIXME randomized add and remove, with listener
	// FIXME recreate the state via the listener, and make sure the recreated state matches the input state
}
