# build + run artifacts
sim-latency
build/
eval-48g/
eval-12c/
# The launchers for these legacy host campaigns are archived under old/, but
# existing local evidence trees remain ignored to prevent accidental commits.
.sim-site/
baseline-runs/
baseline*.json
!testdata/score/baseline.golden.json
run.json
*.csv
providers.yml

# Curated, terminal baseline snapshots are source artifacts rather than run
# scratch. Keep this exception last so their run.json, CSV, providers.yml, and
# baseline JSON files are all versioned.
!baseline/
!baseline/**
