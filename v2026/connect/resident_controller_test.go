package main

/*

func TestHandleContract() {

	// networkA
	// networkB


	residentController := newResidentController(
		ctx context.Context,
	    cancel context.CancelFunc,
	    clientId server.Id,
	    client *connect.Client,
	    residentContractManager *residentContractManager,
	    settings *ExchangeSettings,
	)

	createContract := CREATECONTRACT{
		A, B,
		NO EXISTING
	}
	frames := residentController.handleCreateContract(createContract)

	// one contract
	// 1 total

	createContract = CREATECONTRACT{
		A, B,
		NO EXISTING
	}
	frames = residentController.handleCreateContract(createContract)

	// one contract
	// 1 total


	createContract = CREATECONTRACT{
		A, B,
		ONE EXISTING
	}
	frames = residentController.handleCreateContract(createContract)

	// one contract
	// 2 total


	createContract = CREATECONTRACT{
		A, B,
		TWO EXISTING
	}
	frames = residentController.handleCreateContract(createContract)

	// one contract
	// 3 total


	createContract = CREATECONTRACT{
		A, B,
		NO EXISTING
	}
	frames = residentController.handleCreateContract(createContract)

	// one contract (already exists)
	// 3 total


	// close all contracts

	createContract = CREATECONTRACT{
		A, B,
		NO EXISTING
	}
	frames = residentController.handleCreateContract(createContract)

	// one contract
	// 4 total


	// check balance is 4 contracts less


	// create check insufficient balance error


}



func TestHandleContractCompanion() {

	// networkA
	// networkB


	residentController := newResidentController(
		ctx context.Context,
	    cancel context.CancelFunc,
	    clientId server.Id,
	    client *connect.Client,
	    residentContractManager *residentContractManager,
	    settings *ExchangeSettings,
	)

	createContract := CREATECONTRACT{
		A, B,
		NO EXISTING,
		COMPANION
	}
	frames := residentController.handleCreateContract(createContract)

	// error no origin

	// create origin contract

	createContract = CREATECONTRACT{
		A, B,
		NO EXISTING,
		COMPANION
	}
	frames = residentController.handleCreateContract(createContract)

	// one contract
	// 1 total

	// check balance of origin is 1 contract less
	// check source is at original balance




}

*/
